import { NextResponse } from "next/server";

// API skeleton route for payroll overview
export async function GET() {
  return NextResponse.json({ message: "Get payroll overview placeholder" }, { status: 501 });
}
